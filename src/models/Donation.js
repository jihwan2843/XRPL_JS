class Donation {
  constructor(grantId, sponsor, amount, grantTime) {
    this.grantId = grantId;
    this.sponsor = sponsor;
    this.amount = amount;
    this.grantTime = grantTime;
  }
}

module.exports = Donation;
